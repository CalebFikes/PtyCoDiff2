#!/usr/bin/env python3
"""Obsolete DDPM sampling script stub.

This functionality has been consolidated into `diffusion/sampler.py`.
Import and use `make_sampler` from that module instead of running this script.
"""

raise RuntimeError(
    "Deprecated: use diffusion.sampler.make_sampler(...) instead of this script."
)
